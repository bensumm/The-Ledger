.fills-commit-msg.tmp
.claude/settings.local.json
.claude/worktrees/
pipeline/.cache/
outcomes.json
heartbeat.json

# total-capital: Ben's stated idle-cash balance (cash.mjs) — desk-side, never committed
.capital-state.json

# PM1 probe firing logs (optional per-probe hit/miss ledgers) — desk-side scoring data, never committed
pipeline/probes/*.log

# D0: the Tier-1 SQLite market archive (append-forever raw /1h + /5m observations, ~30-35GB/yr) and
# its WAL/SHM sidecars. Desk-side, machine-local, huge — NEVER committed. Lives OUTSIDE .cache/ so
# cache pruning never touches it.
pipeline/.market-archive.sqlite
pipeline/.market-archive.sqlite-wal
pipeline/.market-archive.sqlite-shm

# SR1 rolled-out suggestion-ledger months (Ben, 2026-08-07): LOCAL-ONLY history. 75MB and growing
# ~13k rows/day, so it is no longer published to this public repo — the archive stays on disk and
# `sync-fills.mjs --publish` deliberately does NOT commit it (see the note at its commit set).
pipeline/suggestions-archive/

# PLAN-OSCILLATION-CYCLE Chunk 4: per-item cycle-expectation state for watch-positions.mjs --cycle.
# Desk-side runtime state (like pipeline/.cache/watch-state.json), rebuilt fresh each pass — NEVER committed.
cycle-watch.json
